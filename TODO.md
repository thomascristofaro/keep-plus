# Development TODO
- remove the single column view in desktop layout, no change layout.
Canghes in the UI CARDS EDITING AND CREATION:
- "Save changes" must be automatically when you do a modification or creation. I don't want click a button
- remove the button "save changes"
- if I click outside the card, close it
- remove the header from the card
- remove the X button for closing the card
- creating a fixed footer for the card with two button:
 - delete, trasformed with an icon
 - close, that close the card
- change the order of the fields:
 - title
 - cover image url
 - content
 - link
 - tags
- In the UI card, I want to remove the titles for every fields, put inside the input box, like placeholder
